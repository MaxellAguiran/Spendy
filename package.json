{
  "name": "maxell-aguiran-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build_sites.mjs",
    "test:unit": "node --test tests/build-sites.test.mjs && python3 -m unittest discover -s tests -p 'test_*.py'",
    "test:browser": "node --test tests/compose-portfolio.test.mjs",
    "validate:html": "html-validate '*.html' 'articles/*.html'",
    "test": "npm run test:unit && npm run validate:html && npm run test:browser"
  },
  "devDependencies": {
    "@axe-core/playwright": "latest",
    "html-validate": "latest",
    "lighthouse": "latest",
    "playwright": "latest"
  }
}
